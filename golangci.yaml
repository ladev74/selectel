version: "2"

linters:
  default: none
  enable:
    - logcheck
  settings:
    custom:
      logcheck:
        type: "module"
        path: github.com/ladev74/linter
        description: checks log messages
#        settings:
#          message: hello